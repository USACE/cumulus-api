### Temporary Directory; Keep README.md; All Other Files .gitignored

On `docker compose -f docker-compose.minio.yml up`, files from this directory (minio/cumulus) are uploaded to local minio bucket "caste-data-develop" with key prefix /cumulus/